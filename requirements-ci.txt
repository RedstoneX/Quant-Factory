# Public, portable dependencies for the GitHub Actions CI test subset.
# VectorBT Pro, local market datasets, and browser binaries are deliberately
# excluded. Their tests require the controlled WSL environment and are not
# represented by a GitHub-hosted CI green result.
exchange-calendars==4.13.2
numpy==2.4.6
pandas==2.3.3
pandas-market-calendars==5.4.0
pyarrow==24.0.0
pytest==9.1.1
requests==2.34.2
PyYAML==6.0.3
